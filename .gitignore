# Gradle
.gradle/
build/
!gradle/wrapper/gradle-wrapper.jar

# Kotlin
.kotlin/

# Local configuration — contains machine-specific SDK paths
local.properties

# Android Studio / IntelliJ
.idea/
*.iml
captures/
.externalNativeBuild/
.cxx/

# Build outputs
*.apk
*.aab
*.ap_
*.dex

# Signing material — never commit these.
# Losing the keystore means never being able to update the published app again,
# so it lives outside git and must be backed up somewhere durable.
*.jks
*.keystore
keystore.properties

# macOS
.DS_Store

# Backend pipeline
backend/data/
backend/public/
backend/.venv/
__pycache__/
*.pyc

# Icon comparison renders — working files, not sources
icon-preview/
